pipeline {
    agent any

    stages {
         
        stage('Staging') {
            steps {
                sh 'docker container kill $(docker ps)'    
                sh 'docker rm $(docker ps -a)'
                sh 'docker rmi $(docker image ls)'
            }
        }
        
    }
}
