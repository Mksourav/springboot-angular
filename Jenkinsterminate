pipeline {
    agent any

    stages {
         
        stage('Staging') {
            steps {
                sh 'docker stop $(docker ps -a -q)'    
                sh 'docker rm $(docker ps -a -q)'
                sh 'docker rmi $(docker image ls)'
            }
        }
        
    }
}
